🚀 API Test

Проект API Test — это REST-API приложение на FastAPI, предназначенное для тестирования и примеров работы с авторизацией, сессиями и внешними сервисами (например, слушателем погодного API).

📌 Основные возможности

🚀 Сервер на FastAPI

🔐 Авторизация и управление сессиями

🌤 Эндпоинты работы с погодой

🧠 Установка

1. Клонирование
git clone https://github.com/Pngroot/api_test.git
cd api_test

2. Виртуальное окружение

Рекомендуется создать виртуальное окружение:

python3 -m venv venv
source venv/bin/activate   # Linux / macOS
venv\Scripts\activate      # Windows

3. Установка зависимостей
pip install -r requirements.txt

🚀 Запуск сервера

Запустить приложение можно через uvicorn:

uvicorn main:app --reload

или с помощью FastAPI в DEV-режиме:

fastapi dev main.py

После запуска сервер будет доступен по умолчанию на:

http://127.0.0.1:8000

📌 Доступные маршруты

🧾 Основные

GET	/ - Возвращает { "message": "Hello World" }
GET	/logout -	Завершает авторизованную сессию

🔐 Авторизация
Маршруты авторизации находятся в отдельной группе (auth_router) — например, логин/регистрация, получение токена и др. (детали см. в src/api/auth.py).

🌦 Погода
Маршруты погоды содержатся в weather_router — работа с внешним API (логика внутри src/api/weather.py).

🗃 База данных и миграции

Проект использует SQLAlchemy + Alembic для работы с базой данных.

Запуск миграций:

alembic revision --autogenerate

alembic upgrade head

⚙️ Конфигурация

Используй файл .env для указания переменных окружения:
